using Dotflix.Controllers;
using Dotflix.Models;
using Dotflix.Models.Contracts.Services;
using System.Collections.Generic;

namespace ApiDotflixTest.ControllerTests
{
    public abstract class BaseLanguageControllerTest
    {/*
        protected readonly List<Language> _language;
        protected readonly Mock<ILanguageService> _mockService;
        protected LanguageController _languageController;

        public BaseLanguageControllerTest(List<Language> languages)
        {
            _language = languages;
            _mockService = new Mock<ILanguageService>();
            _mockService.Setup(x => x.GetAllAsync()).ReturnsAsync(languages);
            _languageController = new LanguageController(_mockService.Object);
        }*/
    }
}
